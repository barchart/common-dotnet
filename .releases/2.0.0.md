**Bug Fixes**

* Preserved non-not-found AWS Secrets Manager errors instead of converting every service failure to `SecretNotFoundException`.
* Rejected undefined numeric currency codes and non-finite exchange rates.

**Technical Enhancements**

* Removed the unused `failureValue` parameter from `Scheduler.BackoffAsync`
* Changed `LimitedSizeQueue` from inheritance to composition.
* Replaced custom date calculations with built-in .NET APIs.
